# Description

Order rule for import

1. Node built-in
2. @nestjs packages
3. External libraries
4. Internal libraries (./, ../, @)

## Command

```bash
# Format src and test
npm run format

# Format a specific file (ex: app.module.ts)
npx prettier --write src/app.module.ts
```
